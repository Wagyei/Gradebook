// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#006400', // Change this to your primary color
    },
    secondary: {
      main: '#CAEEC9', // Change this to your secondary color
    },
  },
});

export default theme;
